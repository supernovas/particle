[package]
name = "particle-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Particle protocol kernel: events, clocks, canonical order, deterministic fold"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
ulid = "1"
